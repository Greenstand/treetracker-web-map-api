apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: treetracker-web-map-api-test-release-ingress
  namespace: default
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: treetracker-web-map-api-test-release-service
          servicePort: 80
